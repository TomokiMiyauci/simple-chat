import { shallowMount } from '@vue/test-utils'
import VOverlayWrapper from '~/components/organisms/VOverlayWrapper'

describe('VOverlayWrapper', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VOverlayWrapper)
    expect(wrapper)
  })
})
