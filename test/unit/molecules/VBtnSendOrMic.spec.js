import { shallowMount } from '@vue/test-utils'
import VBtnSendOrMic from '~/components/molecules/VBtnSendOrMic'

describe('VBtnSendOrMic', () => {
  it('should be ', () => {
    const wrapper = shallowMount(VBtnSendOrMic)
    expect(wrapper)
  })
})
